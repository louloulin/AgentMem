[package]
name = "single-binary-demo"
version = "0.1.0"
edition = "2021"

[dependencies]
agent-mem-deployment = { path = "../../crates/agent-mem-deployment", features = ["full"] }
agent-mem-traits = { path = "../../crates/agent-mem-traits" }
tokio = { version = "1.47", features = ["full"] }
tracing = "0.1"
tracing-subscriber = "0.3"
anyhow = "1.0"

