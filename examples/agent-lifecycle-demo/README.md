# Agent 生命周期演示

本示例演示 AgentMem 中所有 Agent 的 `initialize()` 和 `shutdown()` 方法。

## 功能

- ✅ 创建 5 个不同类型的 Agent
- ✅ 调用 `initialize()` 方法初始化 Agent
- ✅ 调用 `shutdown()` 方法关闭 Agent
- ✅ 展示完整的 Agent 生命周期管理

## 测试的 Agent

1. **CoreAgent** (核心记忆 Agent)
   - 管理长期核心记忆
   - 验证存储连接
   - 加载核心记忆项统计

2. **EpisodicAgent** (情景记忆 Agent)
   - 管理时间序列事件
   - 验证存储连接
   - 查询最近事件

3. **SemanticAgent** (语义记忆 Agent)
   - 管理知识图谱
   - 验证存储连接
   - 加载知识项统计

4. **ProceduralAgent** (程序记忆 Agent)
   - 管理技能和程序
   - 验证存储连接
   - 加载技能/程序统计

5. **WorkingAgent** (工作记忆 Agent)
   - 管理短期工作记忆
   - 验证存储连接
   - 加载会话工作记忆

## 运行方式

```bash
# 从 agentmen 根目录运行
cargo run --example agent-lifecycle-demo

# 或者从示例目录运行
cd examples/agent-lifecycle-demo
cargo run
```

## 预期输出

```
╔══════════════════════════════════════════════════════════════════════╗
║          AgentMem Agent 生命周期演示                                  ║
╚══════════════════════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1️⃣  测试 CoreAgent (核心记忆 Agent)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📌 调用 initialize()...
[INFO] 初始化核心记忆 Agent: core-agent-xxx
[WARN] 未配置核心记忆存储后端，Agent 将以只读模式运行
[INFO] 核心记忆 Agent 初始化完成
✅ CoreAgent 初始化完成

📌 调用 shutdown()...
[INFO] 关闭核心记忆 Agent: core-agent-xxx
[INFO] 核心记忆 Agent 已成功关闭
✅ CoreAgent 关闭完成

... (其他 Agent 类似输出)

╔══════════════════════════════════════════════════════════════════════╗
║          🎉 所有 Agent 生命周期测试完成！                             ║
╚══════════════════════════════════════════════════════════════════════╝

✅ 测试结果:
   - CoreAgent: 初始化 ✅ | 关闭 ✅
   - EpisodicAgent: 初始化 ✅ | 关闭 ✅
   - SemanticAgent: 初始化 ✅ | 关闭 ✅
   - ProceduralAgent: 初始化 ✅ | 关闭 ✅
   - WorkingAgent: 初始化 ✅ | 关闭 ✅
```

## 说明

- 所有 Agent 都未配置存储后端，因此以只读模式运行
- 如需测试存储集成，请配置相应的存储后端（PostgreSQL, LibSQL 等）
- 详细日志使用中文输出，便于理解

## 相关文档

- [mem15.md](../../../doc/technical-design/memory-systems/mem15.md) - 生产级实现计划
- [mem15-phase-status.md](../../../doc/technical-design/memory-systems/mem15-phase-status.md) - Phase 实施状态
- [Agent 实现代码](../../crates/agent-mem-core/src/agents/) - Agent 源代码

## 实现细节

每个 Agent 的 `initialize()` 方法:
1. 检查是否已初始化（避免重复初始化）
2. 记录初始化日志（中文）
3. 如果配置了存储后端，验证连接并加载统计信息
4. 初始化 Agent 上下文（清零统计）
5. 设置 `initialized = true`

每个 Agent 的 `shutdown()` 方法:
1. 检查是否已初始化（避免重复关闭）
2. 记录关闭日志（中文）
3. 如果有存储后端，记录最终统计信息
4. 清理 Agent 上下文
5. 设置 `initialized = false`

## 代码证据

- CoreAgent: `crates/agent-mem-core/src/agents/core_agent.rs` (Line 516-594)
- EpisodicAgent: `crates/agent-mem-core/src/agents/episodic_agent.rs` (Line 402-488)
- SemanticAgent: `crates/agent-mem-core/src/agents/semantic_agent.rs` (Line 527-610)
- ProceduralAgent: `crates/agent-mem-core/src/agents/procedural_agent.rs` (Line 382-465)
- WorkingAgent: `crates/agent-mem-core/src/agents/working_agent.rs` (Line 256-333)

## Git Commit

- Commit: f177015
- 日期: 2025-10-12
- 消息: "feat: 实现 5 个 Agent 的 initialize() 和 shutdown() 方法"

