# AgentMem 前端测试和部署指南

**版本**: v2.1  
**日期**: 2025-10-01  
**状态**: 准备测试

---

## 📋 前置要求

### 1. 安装 Node.js

```bash
# macOS (使用 Homebrew)
brew install node

# 或者从官网下载
# https://nodejs.org/

# 验证安装
node --version  # 应该显示 v20.x.x 或更高
npm --version   # 应该显示 v10.x.x 或更高
```

### 2. 安装依赖

```bash
cd agentmen/agentmem-website

# 使用 npm
npm install

# 或使用 pnpm (更快)
npm install -g pnpm
pnpm install
```

---

## 🧪 测试步骤

### 步骤 1: 编译测试

```bash
# 构建生产版本
npm run build

# 预期输出:
# ✓ Compiled successfully
# ✓ Collecting page data
# ✓ Generating static pages
# ✓ Finalizing page optimization
```

**验收标准**:
- ✅ 编译成功，无错误
- ✅ 无 TypeScript 类型错误
- ✅ 无 ESLint 警告

**常见问题**:

1. **类型错误**: 检查 `api-client.ts` 中的类型定义
2. **导入错误**: 确保所有组件路径正确
3. **依赖缺失**: 运行 `npm install` 安装缺失的包

---

### 步骤 2: 开发服务器测试

```bash
# 启动开发服务器
npm run dev

# 服务器应该在 http://localhost:3000 启动
```

**测试清单**:

#### 2.1 访问管理界面
- [ ] 打开 http://localhost:3000/admin
- [ ] 验证侧边栏导航显示正确
- [ ] 验证深色模式切换工作正常

#### 2.2 Dashboard 页面
- [ ] 访问 http://localhost:3000/admin
- [ ] 验证统计卡片显示
- [ ] 验证活动列表显示

#### 2.3 Agents 管理页面
- [ ] 访问 http://localhost:3000/admin/agents
- [ ] 点击 "Create Agent" 按钮
- [ ] 填写表单并提交
- [ ] 验证 Agent 卡片显示
- [ ] 测试删除 Agent 功能

#### 2.4 Chat 界面
- [ ] 访问 http://localhost:3000/admin/chat
- [ ] 选择一个 Agent
- [ ] 发送测试消息
- [ ] 验证消息显示正确
- [ ] 验证自动滚动工作

#### 2.5 Memories 管理页面
- [ ] 访问 http://localhost:3000/admin/memories
- [ ] 选择一个 Agent
- [ ] 验证 Memories 列表显示
- [ ] 测试类型过滤
- [ ] 测试搜索功能
- [ ] 测试删除功能

#### 2.6 Users 管理页面
- [ ] 访问 http://localhost:3000/admin/users
- [ ] 验证用户列表显示
- [ ] 验证用户卡片信息正确

#### 2.7 Settings 页面
- [ ] 访问 http://localhost:3000/admin/settings
- [ ] 修改 API URL
- [ ] 修改 API Key
- [ ] 点击保存
- [ ] 验证设置已保存到 localStorage

---

### 步骤 3: API 集成测试

**前置条件**: 确保后端 API 服务器正在运行

```bash
# 在另一个终端启动后端服务器
cd agentmen
cargo run --bin agent-mem-server

# 服务器应该在 http://localhost:8080 启动
```

**测试清单**:

#### 3.1 配置 API
1. 打开 Settings 页面
2. 设置 API URL: `http://localhost:8080`
3. 设置 API Key (如果需要)
4. 保存设置

#### 3.2 测试 Agent API
- [ ] 创建新 Agent
- [ ] 验证 Agent 出现在列表中
- [ ] 查看 Agent 详情
- [ ] 更新 Agent 状态
- [ ] 删除 Agent

#### 3.3 测试 Memory API
- [ ] 为 Agent 创建 Memory
- [ ] 查看 Memory 列表
- [ ] 搜索 Memories
- [ ] 删除 Memory

#### 3.4 测试 Chat API
- [ ] 发送消息给 Agent
- [ ] 验证 Agent 响应
- [ ] 查看消息历史

---

### 步骤 4: 响应式测试

**测试不同屏幕尺寸**:

```bash
# 在浏览器中打开开发者工具 (F12)
# 切换到设备模拟模式
```

**测试清单**:
- [ ] 手机 (375px): 侧边栏应该折叠
- [ ] 平板 (768px): 布局应该调整
- [ ] 桌面 (1024px+): 完整布局显示

---

### 步骤 5: 性能测试

```bash
# 运行 Lighthouse 测试
npm run build
npm run start

# 在 Chrome 中打开 http://localhost:3000/admin
# 打开开发者工具 > Lighthouse
# 运行性能测试
```

**目标指标**:
- Performance: > 90
- Accessibility: > 90
- Best Practices: > 90
- SEO: > 80

---

## 🐛 Bug 修复清单

### 已知问题

1. **API 客户端类型问题**
   - 问题: 某些 API 响应类型可能不匹配
   - 解决: 检查 `api-client.ts` 中的类型定义

2. **深色模式闪烁**
   - 问题: 页面加载时可能出现主题闪烁
   - 解决: 使用 `next-themes` 的 `suppressHydrationWarning`

3. **WebSocket 连接**
   - 问题: Chat 界面目前使用模拟响应
   - 解决: 实现真实的 WebSocket 连接

### 修复步骤

如果发现编译错误，按以下步骤修复：

1. **查看错误日志**
   ```bash
   npm run build 2>&1 | tee build.log
   ```

2. **修复类型错误**
   - 检查 TypeScript 类型定义
   - 确保所有导入路径正确

3. **修复 ESLint 警告**
   ```bash
   npm run lint
   ```

4. **重新测试**
   ```bash
   npm run build
   npm run dev
   ```

---

## 📦 部署指南

### 生产构建

```bash
# 构建生产版本
npm run build

# 启动生产服务器
npm run start

# 服务器在 http://localhost:3000 启动
```

### Docker 部署

```dockerfile
# Dockerfile
FROM node:20-alpine AS builder

WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:20-alpine AS runner
WORKDIR /app

COPY --from=builder /app/next.config.ts ./
COPY --from=builder /app/public ./public
COPY --from=builder /app/.next ./.next
COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/package.json ./package.json

EXPOSE 3000
CMD ["npm", "start"]
```

```bash
# 构建 Docker 镜像
docker build -t agentmem-frontend .

# 运行容器
docker run -p 3000:3000 -e NEXT_PUBLIC_API_URL=http://localhost:8080 agentmem-frontend
```

### 环境变量

创建 `.env.local` 文件：

```bash
# API 配置
NEXT_PUBLIC_API_URL=http://localhost:8080

# 可选: 启用调试
NEXT_PUBLIC_DEBUG=true
```

---

## ✅ 验收标准

### 编译测试
- [ ] `npm run build` 成功
- [ ] 无 TypeScript 错误
- [ ] 无 ESLint 警告
- [ ] 构建输出正常

### 功能测试
- [ ] 所有页面可以访问
- [ ] 所有按钮和链接工作正常
- [ ] 表单提交正常
- [ ] 数据加载和显示正常
- [ ] 错误处理正常

### 集成测试
- [ ] 可以连接到后端 API
- [ ] 可以创建/读取/更新/删除 Agents
- [ ] 可以管理 Memories
- [ ] 可以查看 Users
- [ ] 设置可以保存

### 性能测试
- [ ] Lighthouse Performance > 90
- [ ] 首次加载时间 < 3s
- [ ] 交互响应时间 < 100ms

### 响应式测试
- [ ] 手机端布局正常
- [ ] 平板端布局正常
- [ ] 桌面端布局正常

---

## 📝 测试报告模板

```markdown
# 前端测试报告

**测试日期**: YYYY-MM-DD
**测试人员**: [姓名]
**版本**: v2.1

## 编译测试
- [ ] 通过 / [ ] 失败
- 错误信息: [如果有]

## 功能测试
- Dashboard: [ ] 通过 / [ ] 失败
- Agents: [ ] 通过 / [ ] 失败
- Chat: [ ] 通过 / [ ] 失败
- Memories: [ ] 通过 / [ ] 失败
- Users: [ ] 通过 / [ ] 失败
- Settings: [ ] 通过 / [ ] 失败

## 集成测试
- API 连接: [ ] 通过 / [ ] 失败
- Agent CRUD: [ ] 通过 / [ ] 失败
- Memory 管理: [ ] 通过 / [ ] 失败

## 性能测试
- Lighthouse Score: [分数]
- 加载时间: [秒]

## 发现的问题
1. [问题描述]
2. [问题描述]

## 总体评价
[ ] 通过所有测试，可以发布
[ ] 有小问题，需要修复
[ ] 有严重问题，需要重新测试
```

---

## 🚀 下一步

完成测试后：

1. ✅ 更新 `mem11.md` 文档
2. ✅ 提交代码到 Git
3. ✅ 创建 Git tag: `v2.1-beta`
4. ✅ 发布 Release Notes
5. ✅ 通知团队测试

---

**文档版本**: v1.0  
**最后更新**: 2025-10-01

