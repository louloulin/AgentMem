# AgentMem 快速开始

欢迎来到 AgentMem！本指南将帮助你在 5 分钟内上手 AgentMem。

## 安装

### Rust

将 AgentMem 添加到你的 `Cargo.toml`:

```bash
cargo add agentmem
```

或在 `Cargo.toml` 中手动添加:

```toml
[dependencies]
agentmem = "2.0"
tokio = { version = "1", features = ["full"] }
```

### Python

```bash
pip install agentmem
```

## 5 分钟上手

### 场景：为 AI 助手添加记忆能力

我们将构建一个简单的 AI 助手，能够记住用户偏好并进行个性化推荐。

### Rust 示例

```rust
use agentmem::{Memory, MemoryType, GetAllOptions};

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    // ========================================
    // 步骤 1: 创建实例（零配置）
    // ========================================
    println!("🚀 初始化 AgentMem...");

    let memory = Memory::new().await?;

    println!("✅ 初始化成功！\n");

    // ========================================
    // 步骤 2: 添加记忆
    // ========================================
    println!("📝 添加用户偏好...");

    // 添加语义记忆（长期知识）
    memory.add(
        "用户喜欢科幻电影，尤其是《星际穿越》和《银翼杀手》",
        None,  // 不指定 session
        MemoryType::SEMANTIC
    ).await?;
    println!("✅ 已添加: 电影偏好");

    memory.add(
        "用户的工作是软件工程师，主要使用 Rust 和 Python",
        None,
        MemoryType::SEMANTIC
    ).await?;
    println!("✅ 已添加: 职业信息");

    memory.add(
        "用户住在上海，喜欢在周末去咖啡厅工作",
        None,
        MemoryType::SEMANTIC
    ).await?;
    println!("✅ 已添加: 生活习惯\n");

    // ========================================
    // 步骤 3: 搜索记忆
    // ========================================
    println!("🔍 搜索用户偏好...");

    let query = "用户喜欢什么样的电影？";
    let results = memory.search(query).await?;

    println!("✅ 找到 {} 条相关记忆:\n", results.len());

    for (i, mem) in results.iter().enumerate() {
        let score = mem.score.unwrap_or(0.0);
        println!("  {}. {} (相似度: {:.2})", i + 1, mem.content, score);
    }

    // ========================================
    // 步骤 4: 获取所有记忆
    // ========================================
    println!("\n📚 获取所有记忆...");

    let all_memories = memory.get_all(GetAllOptions::default()).await?;

    println!("✅ 共有 {} 条记忆:\n", all_memories.len());

    for (i, mem) in all_memories.iter().enumerate() {
        println!("  {}. {}", i + 1, mem.content);
    }

    // ========================================
    // 步骤 5: 智能去重演示
    // ========================================
    println!("\n🧠 演示智能去重...");

    memory.add(
        "用户喜欢看《星际穿越》和《银翼杀手》这类科幻片",
        None,
        MemoryType::SEMANTIC
    ).await?;

    println!("✅ 添加重复内容，系统自动去重\n");

    // 验证去重
    let updated_memories = memory.get_all(GetAllOptions::default()).await?;
    println!("📊 去重后仍有 {} 条记忆（未增加）", updated_memories.len());

    println!("\n🎉 快速开始完成！");

    Ok(())
}
```

### Python 示例

```python
from agentmem import Memory, MemoryType

async def main():
    # ========================================
    # 步骤 1: 创建实例（零配置）
    # ========================================
    print("🚀 初始化 AgentMem...")

    memory = await Memory.new()

    print("✅ 初始化成功！\n")

    # ========================================
    # 步骤 2: 添加记忆
    # ========================================
    print("📝 添加用户偏好...")

    # 添加语义记忆（长期知识）
    await memory.add(
        "用户喜欢科幻电影，尤其是《星际穿越》和《银翼杀手》",
        memory_type=MemoryType.SEMANTIC
    )
    print("✅ 已添加: 电影偏好")

    await memory.add(
        "用户的工作是软件工程师，主要使用 Rust 和 Python",
        memory_type=MemoryType.SEMANTIC
    )
    print("✅ 已添加: 职业信息")

    await memory.add(
        "用户住在上海，喜欢在周末去咖啡厅工作",
        memory_type=MemoryType.SEMANTIC
    )
    print("✅ 已添加: 生活习惯\n")

    # ========================================
    # 步骤 3: 搜索记忆
    # ========================================
    print("🔍 搜索用户偏好...")

    query = "用户喜欢什么样的电影？"
    results = await memory.search(query)

    print(f"✅ 找到 {len(results)} 条相关记忆:\n")

    for i, mem in enumerate(results):
        print(f"  {i+1}. {mem.content} (相似度: {mem.score:.2f})")

    # ========================================
    # 步骤 4: 获取所有记忆
    # ========================================
    print("\n📚 获取所有记忆...")

    all_memories = await memory.get_all()

    print(f"✅ 共有 {len(all_memories)} 条记忆:\n")

    for i, mem in enumerate(all_memories):
        print(f"  {i+1}. {mem.content}")

    # ========================================
    # 步骤 5: 智能去重演示
    # ========================================
    print("\n🧠 演示智能去重...")

    await memory.add(
        "用户喜欢看《星际穿越》和《银翼杀手》这类科幻片",
        memory_type=MemoryType.SEMANTIC
    )

    print("✅ 添加重复内容，系统自动去重\n")

    # 验证去重
    updated_memories = await memory.get_all()
    print(f"📊 去重后仍有 {len(updated_memories)} 条记忆（未增加）")

    print("\n🎉 快速开始完成！")

if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
```

## 运行示例

### Rust

```bash
# 1. 设置环境变量（可选，用于启用智能功能）
export OPENAI_API_KEY=sk-...
# 或
export ANTHROPIC_API_KEY=sk-...

# 2. 运行
cargo run
```

### Python

```bash
# 1. 设置环境变量（可选，用于启用智能功能）
export OPENAI_API_KEY=sk-...

# 2. 运行
python main.py
```

## 输出示例

```
🚀 初始化 AgentMem...
✅ 初始化成功！

📝 添加用户偏好...
✅ 已添加: 电影偏好
✅ 已添加: 职业信息
✅ 已添加: 生活习惯

🔍 搜索用户偏好...
✅ 找到 3 条相关记忆:

  1. 用户喜欢科幻电影，尤其是《星际穿越》和《银翼杀手》 (相似度: 0.95)
  2. 用户的工作是软件工程师，主要使用 Rust 和 Python (相似度: 0.32)
  3. 用户住在上海，喜欢在周末去咖啡厅工作 (相似度: 0.18)

📚 获取所有记忆...
✅ 共有 3 条记忆:

  1. 用户喜欢科幻电影，尤其是《星际穿越》和《银翼杀手》
  2. 用户的工作是软件工程师，主要使用 Rust 和 Python
  3. 用户住在上海，喜欢在周末去咖啡厅工作

🧠 演示智能去重...
✅ 添加重复内容，系统自动去重

📊 去重后仍有 3 条记忆（未增加）

🎉 快速开始完成！
```

## 无 API Key 模式

如果你暂时没有 API Key，可以使用**简单模式**（禁用智能功能）：

### Rust

```rust
use agentmem::{Memory, MemoryType};

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    // 禁用智能功能（无需 API Key）
    let memory = Memory::builder()
        .infer(false)  // 禁用智能推断
        .build()
        .await?;

    // 基本功能仍然可用
    memory.add("用户喜欢咖啡").await?;

    let results = memory.search("饮品").await?;

    Ok(())
}
```

### Python

```python
from agentmem import Memory

async def main():
    # 禁用智能功能（无需 API Key）
    memory = await Memory.builder() \
        .infer(False) \
        .build()

    # 基本功能仍然可用
    await memory.add("用户喜欢咖啡")

    results = await memory.search("饮品")

if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
```

**注意**: 简单模式下，以下功能不可用：
- 自动事实提取
- 智能去重
- 冲突解决
- 语义嵌入（搜索将使用关键词匹配）

## 下一步

现在你已经完成了快速开始，接下来可以：

- [基础概念](tutorials/basic_concepts.md) - 理解 AgentMem 的核心概念
- [记忆管理](tutorials/memory_management.md) - 学习如何管理记忆
- [语义搜索](tutorials/semantic_search.md) - 深入了解搜索功能
- [生产部署](tutorials/production.md) - 部署到生产环境

## 常见问题

### Q: 如何在生产环境中使用？

A: 参考我们的[生产部署指南](tutorials/production.md)，了解数据库配置、性能优化等。

### Q: 支持哪些数据库？

A: AgentMem 支持多种存储后端：
- LibSQL（默认，零配置）
- PostgreSQL
- SQLite
- Redis（缓存）
- 云存储（S3, GCS）

### Q: 如何处理大量数据？

A: AgentMem 内置了批量操作和流式处理支持。参考[性能优化指南](tutorials/production.md#性能优化)。

### Q: 可以在浏览器中使用吗？

A: 可以！我们提供了 WebAssembly 支持。参考 [WASM 示例](../examples/wasm/)。

## 获取帮助

- 📖 [完整文档](https://docs.agentmem.dev)
- 💬 [GitHub Discussions](https://github.com/agentmem/agentmem/discussions)
- 🐛 [报告问题](https://github.com/agentmem/agentmem/issues)
- 📧 联系我们: support@agentmem.dev
